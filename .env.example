# =============================================================================
# Creative Reviewer — Environment Variables
# =============================================================================
# Copy to .env for local dev. In production, use Google Secret Manager.

# Environment mode ("development" or "production")
ENVIRONMENT=development

# --- Google OAuth (required for login) ---
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# --- Session ---
# MUST be a random 64-char hex string in production.
# Generate: python3 -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET=change-me-in-production

# --- Stripe billing (required for token purchases) ---
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_1000=
STRIPE_PRICE_3000=

# --- Database ---
# SQLite for local dev; PostgreSQL for production.
# PostgreSQL: postgresql://user:pass@/dbname?host=/cloudsql/PROJECT:REGION:INSTANCE
DATABASE_URL=sqlite:///data/app.db

# --- Google Cloud ---
ABCD_KG_API_KEY=

# --- SMTP email ---
# Works with SendGrid, Gmail SMTP relay, Amazon SES, Mailgun, etc.
# Falls back to console logging if not set.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@aicreativereview.com

# --- Slack notifications (optional) ---
SLACK_WEBHOOK_URL=

# --- Admin signup notification email (optional) ---
# If set, an email is sent to this address on every new signup.
ADMIN_NOTIFICATION_EMAIL=

# --- Public URL (used for report links, OAuth redirects, Stripe callbacks) ---
PUBLIC_BASE_URL=http://localhost:8080

# --- CORS ---
# Comma-separated allowed origins. Leave empty in production.
ALLOWED_ORIGINS=http://localhost:8080,http://localhost:3000

# --- FFmpeg ---
IMAGEIO_FFMPEG_EXE=/opt/homebrew/bin/ffmpeg
